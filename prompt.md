# 项目构建

在这个目录里构建一个python包。
它是 aws boto3 的浅封装，用来解决特定的云服务商提供的对象存储错误问题。

经过校验执行成功的读写示例在verified目录下，以供参考。

主要问题在于请求头中的签名。

这个python包需要提供正规的包定义，能够被 python 的内置 build 工具打包
并上传到 pypi。

我们需要提供类似 boto3 的调用接口，与原版尽可能一致，在接口下通过 httpx 
封装请求过程。

利用httpx的异步能力，提供同步和异步的版本。

提供测试代码。我有必要的云端环境用于测试。